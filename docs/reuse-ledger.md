# Reuse Ledger

Treat old code as source material, not inheritance.

| Component | Source commit/file | Why retained | What was removed | New acceptance proof |
|---|---|---|---|---|
| Form lexer and composite-boundary sketches | archive/pre-reboot-2026-08-04, `crates/conduit-panel/src/document.rs` plus checked export concepts | Exact source, lossless tokens, UTF-8 byte spans, line/column locations, recursive form blocks, and explicit exports as the sole composite boundary are sound S3 seams | Old full grammar, modules/packages, formatter, UI recovery AST, implicit/generated faces, UI coupling, and panel-era compatibility | `conduit-form` exact round-trip/token-slice/recovery/source-token-depth bounds, standalone/nested checked-boundary equality, and missing/duplicate export denial; `conduit-composite` derives the advertised contract from that boundary and plans an ordinary parent cord against it |
| Bounded cord sketch | archive/pre-reboot-2026-08-04, hosted scheduler | Item/byte pressure and port-aware stepping are useful S1 quarry material | Production stack and broad node registry | conduit-kernel capacity-one pressure, atomic join rollback, retained-state latest, transactional fanout, per-port closure, cancellation, stable hosted allocation shape, and hosted/fixed equivalence vectors |
| Reboot exact-plan commitments | reboot-prototype-2026-08-04, conduit-core/planner/runtime plus archived `PinnedDescriptor`/`ExecutionProfile` and source/checked/expanded vocabulary | Host, boot, generation, implementation, artifact, fragment, immutable semantic revision, execution profile, separated form identities, and explicit bounded-policy concepts are useful | Single-value-kind capabilities, conflated form identity, transport-name planning claims, and the archived broad policy/runtime stack | S2 vectors separate source/checked/expanded identity; bind exact kind revisions, execution profiles, every port, startup/cancellation/terminal policy, and evidence item/byte budgets; allocate fixed hosted evidence slots; reject cycles, observation loss, post-seal mutations, and resealed lies; remaining plan facts stay open |
| Browser/Pico/relay simulations | reboot-prototype-2026-08-04, fixtures/browser-sim and fixtures/pico-sim | Fast deterministic conformance data | Host, firmware, socket, and physical-acceptance naming | Workspace tests plus WASM/Thumb compile gates, classified only as simulation |
| Port-aware kernel protocol/storage | archive/pre-reboot-2026-08-04, crates/conduit-core/src/scheduler.rs and crates/conduit-embedded/src/lib.rs | Numeric ports, plan-admitted host operations, fixed storage, and deterministic evidence are sound quarry concepts | Old broad plan schema, driver catalog, policy stack, and separate hosted/embedded APIs | conduit-kernel hosted/fixed full public-Operation conformance form, storage and scheduler equivalence, exact-route admission, correlated host completion, late-completion rejection, pressure, closure, cancellation, stable allocation shape, and Thumb vectors |
| Countable resource reservation | archive/pre-reboot-2026-08-04, crates/conduit-core/src/execution_plan.rs `PlanResourceBudget` and crates/conduit-core/src/host.rs `ReportResource` | Finite capacity, exact plan allocation, and availability distinct from authority are sound S2 concepts | Seven-dimensional budget, lease/pool/transition stacks, unmeasured RAM claims, and broad runtime policy | S2 planner resolves exact boot-scoped pool/class/unit bindings, rejects malformed/ambiguous/exhausted offers, hosted preparation recomputes capability/implementation truth and aggregate reservations, and explicit release returns capacity |
| Scoped authority binding | archive/pre-reboot-2026-08-04, crates/conduit-core/src/authority.rs `EffectRequirement`, `AuthorityGrant`, and `ResolvedAuthorityBinding` | Availability separate from permission, externally supplied immutable grants, exact action/subject scope, and denial before effects are sound S2 concepts | Time/expiry, revocation, delegation, constraints, audit, secret material, policy stacks, and broad effect catalog | S2 planner binds one exact host/boot/capability/host-operation/subject grant, preparation revalidates current grant and implementation truth, every field has mutation negatives, and an ungranted subject emits no platform effect |
| Observed remote link binding | archive/pre-reboot-2026-08-04, crates/conduit-core/src/distributed.rs `PlanDistributedCord` and crates/conduit-runtime/src/transport.rs `ResolvedTransportSelection` | A remote cord must pin directional endpoints, initialized provider identity, credential/authority references, current availability, and finite limits independently of semantic transport names | Carrier configuration, broad peer/passport/delegation schema, artifacts/profiles, security modes, sessions, retries, reconnect, memory accounting stack, and Zenoh-specific behavior | S2 planner resolves one exact ready boot-scoped `LinkBinding`, rejects enum-only/missing/stale/unavailable/ambiguous/underbounded links, hashes every field, preparation requires the exact current observation, and Observatory renders it without promoting fixtures into sockets |
