# Reuse Ledger

Treat old code as source material, not inheritance.

| Component | Source commit/file | Why retained | What was removed | New acceptance proof |
|---|---|---|---|---|
| Read-only Observatory projection | archive/pre-reboot-2026-08-04 Observatory table and evidence concepts plus the reboot report-schema prototype | Separate host/boot/link/plan/Play/placement/presentation/evidence identities, structured operational tables, and explicit bounded history remain useful | Central Realm/fleet database, membership-derived runtime truth, UI-owned state, controls, discovery, authority, planning, lifecycle ownership, synthetic production fleet, and graph-only inspection | `ObservatorySnapshot` validates neutral host/capability/link/plan/Play/pressure/evidence/retention reports without `conduit-realm`; actual std execution writes a bounded versioned snapshot; the read-only CLI renders complete tables, rejects tampered boot identity, preserves distinct operational states and failure/terminal facts, and displays host plus snapshot retention gaps |
| Form lexer and composite-boundary sketches | archive/pre-reboot-2026-08-04, `crates/conduit-panel/src/document.rs` plus checked export concepts | Exact source, lossless tokens, UTF-8 byte spans, line/column locations, recursive form blocks, and explicit exports as the sole composite boundary are sound S3 seams | Old full grammar, modules/packages, formatter, UI recovery AST, implicit/generated faces, UI coupling, connection-shaped exports, and panel-era compatibility | `conduit-form` exact round-trip/token-slice/recovery/source-token-depth bounds; standalone/nested checked-boundary equality; recursively path-bound expanded identities; expansion and face mutation denial; named multi-kind and zero-sided face contracts; `conduit-planner` ordinary parent planning; and `conduit-composite` two-kind value/pressure/closure/cancellation/failure/terminal-evidence execution through endpoint-derived faces independent of child cords |
| Bounded cord sketch | archive/pre-reboot-2026-08-04, hosted scheduler | Item/byte pressure and port-aware stepping are useful S1 quarry material | Production stack and broad node registry | conduit-kernel capacity-one pressure, atomic join rollback, retained-state latest, transactional fanout, per-port closure, cancellation, stable hosted allocation shape, and hosted/fixed equivalence vectors |
| Reboot exact-plan commitments | reboot-prototype-2026-08-04, conduit-core/planner/runtime plus archived `PinnedDescriptor`/`ExecutionProfile` and source/checked/expanded vocabulary | Host, boot, generation, implementation, artifact, fragment, immutable semantic revision, execution profile, separated form identities, and explicit bounded-policy concepts are useful | Single-value-kind capabilities, conflated form identity, transport-name planning claims, archived broad policy/runtime stack, and the production std fallback pump | S2 vectors separate source/checked/expanded identity; bind exact kind revisions, execution profiles, every port, startup/cancellation/terminal policy, and evidence item/byte budgets; reject cycles, observation loss, post-seal mutations, and resealed lies; installed Signal pair, three-sink fan-out, and typed multi-value std forms lower to reversible numeric kernel tables, reserve exact resources, and execute with measured zero-allocation activation; unsupported production std forms fail closed while simulation compatibility is explicitly named |
| Browser/Pico/relay simulations | reboot-prototype-2026-08-04, fixtures/browser-sim and fixtures/pico-sim | Fast deterministic conformance data | Host, firmware, socket, and physical-acceptance naming | Workspace tests plus WASM/Thumb compile gates, classified only as simulation |
| Port-aware kernel protocol/storage | archive/pre-reboot-2026-08-04, crates/conduit-core/src/scheduler.rs and crates/conduit-embedded/src/lib.rs | Numeric ports, plan-admitted host operations, fixed storage, and deterministic evidence are sound quarry concepts | Old broad plan schema, driver catalog, policy stack, and separate hosted/embedded APIs | conduit-kernel hosted/fixed full public-Operation conformance form, storage and scheduler equivalence, exact-route admission, correlated host completion, late-completion rejection, pressure, closure, cancellation, stable allocation shape, and Thumb vectors |
| Countable resource reservation | archive/pre-reboot-2026-08-04, crates/conduit-core/src/execution_plan.rs `PlanResourceBudget` and crates/conduit-core/src/host.rs `ReportResource` | Finite capacity, exact plan allocation, and availability distinct from authority are sound S2 concepts | Seven-dimensional budget, lease/pool/transition stacks, unmeasured RAM claims, and broad runtime policy | S2 planner resolves exact boot-scoped pool/class/unit bindings, rejects malformed/ambiguous/exhausted offers, hosted preparation recomputes capability/implementation truth and aggregate reservations, and explicit release returns capacity |
| Scoped authority binding | archive/pre-reboot-2026-08-04, crates/conduit-core/src/authority.rs `EffectRequirement`, `AuthorityGrant`, and `ResolvedAuthorityBinding` | Availability separate from permission, externally supplied immutable grants, exact action/subject scope, and denial before effects are sound S2 concepts | Time/expiry, revocation, delegation, constraints, audit, secret material, policy stacks, and broad effect catalog | S2 planner binds one exact host/boot/capability/host-operation/subject grant, preparation revalidates current grant and implementation truth, every field has mutation negatives, and an ungranted subject emits no platform effect |
| Observed remote link binding | archive/pre-reboot-2026-08-04, crates/conduit-core/src/distributed.rs `PlanDistributedCord` and crates/conduit-runtime/src/transport.rs `ResolvedTransportSelection` | A remote cord must pin directional endpoints, initialized provider identity, credential/authority references, current availability, and finite limits independently of semantic transport names | Carrier configuration, broad peer/passport/delegation schema, artifacts/profiles, security modes, sessions, retries, reconnect, memory accounting stack, and Zenoh-specific behavior | S2 planner resolves one exact ready boot-scoped `LinkBinding`, rejects enum-only/missing/stale/unavailable/ambiguous/underbounded links, hashes every field, preparation requires the exact current observation, and Observatory renders it without promoting fixtures into sockets |
| Remote-cord and live WebSocket session seam | archive/pre-reboot-2026-08-04 distributed/transport concepts plus current `conduit-kernel` remote boundary | Exact boot-scoped endpoints and provider identity, source ownership until exact delivery, bounded pressure, and an explicit control lifecycle are sound distributed-kernel seams | Generalized transport/security policy, retry and discovery stacks, TLS/public-network claims, reconnection, legacy pumps, and carrier-owned scheduling | exact main `a1f479dfa58b8537427b5747da73795628504913`, workflow `31031406945`: exact distributed fragments execute in std and browser/WASM kernels while one bounded binary loopback WebSocket carries sixteen ordered values through one-item/nine-byte limits, one real `Full` and same-sequence retry, terminal evidence, stable capacities, zero retained/in-flight values, and fail-closed identity/frame/lifecycle negatives |
| Runtime execution identity vocabulary | archive/pre-reboot-2026-08-04, `crates/conduit-runtime/src/session.rs` `ExactRunIdentity` and exact evidence concepts | A sealed plan, one activation, each evidence item, and each platform presentation require distinct correlated identities | Archived session registry, pumps, provider authority stack, batch commit protocol, timestamps, and broad evidence schema | no-std core binding vectors cover plan/play/evidence/presentation separation and boot/sequence changes; hosted runtime rejects wrong presentation completion identity; Observatory renders runtime-issued evidence/play/presentation references |
| Browser DOM manifestation boundary | archive/pre-reboot-2026-08-04, `browser/conduit-browser-host.mjs` and its browser vector page; current `hosts/std/src/kernel_signal.rs` hosted-kernel installation pattern | Browser execution identity must remain separate from presentation UI, adapter-owned queues must be finite, and browser/std execution must share the port-aware scheduler and exact-plan lowering boundary | Seven placement modes, membership/status policy, supervision/pools, artifact loading, service workers, audio, WebGPU, cross-engine matrix, legacy `HostRuntime`, and synchronous stdout/thread adapters | exact main `b7852eed1e784a27dcd78e700b2f89ddc01bc097`, workflow `31022565054`: one Chromium page instantiates two independent Rust/WASM planner/kernel hosts for the unchanged local signal form; each drives real timers and its own DOM host through fixed 4,096-byte request/completion frames, retains sixteen item/byte-bounded receipts, preserves the full identity chain, holds sealed capacity stable, terminates honestly, and rejects duplicate, malformed, overflow, cancellation, evidence-exhaustion, platform-failure, and wrong-identity cases without retries |
| Embedded fixed-image model and renderer | archive/pre-reboot-2026-08-04, `crates/conduit-embedded-build/src/lib.rs` generated static node/cord/host-operation models, deterministic renderer, and finite range/error discipline | A host-side build seam must validate one exact current lowered fragment and emit deterministic allocator-free Rust tables for constrained firmware | Archived `ExecutionPlan`, reference plans, scheduler, leases, policy stack, HIL protocol, and hand-authored firmware topology; current code consumes only `PlanFragment` and `LoweredPlanFragment` | `conduit-embedded-build` structural, bound, identity, inconsistent-lowering, and unsupported-remote negatives; unchanged `examples/signal-demo.form` checks, plans, lowers, and generates within exact Pico bounds; firmware Thumb build consumes the generated tables; exact main `0a99f4d75a2ef38cb63dcae474288b3eca429e94`, workflow `31062734745` passed the full check and browser jobs |
