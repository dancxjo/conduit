form morse_network {
    # Run once; each later portable key transition is mapped and manifested.
    keyboard: input/keyboard
    keymap: input/keymap
    morse: text/morse(120)
    light: presentation/indicator

    keyboard.key > keymap.key
    keymap.text > morse > light
}
