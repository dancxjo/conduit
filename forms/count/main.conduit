form count (
    start: Count = 0
    bump: Tick...| > value: $Count
) {
    gear: state/count(start)
    bump > gear.bump
    gear.value > value
}

form count-demo {
    clock: time/every(1s)
    count: count(2)
    show: presentation/count
    clock > count > show
}
