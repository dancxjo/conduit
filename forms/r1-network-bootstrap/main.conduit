form r1-network-bootstrap {
    credentials: network/credentials
    join: network/join
    attachment_sign: network/attachment-sign

    credentials.request > join.request
    join.attachment > attachment_sign.attachment
}
