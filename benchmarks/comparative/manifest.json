{
  "schema": "conduit.comparative-benchmark-manifest",
  "schema_version": 0,
  "fixture_revision": 0,
  "issues": [243, 244, 245, 248, 249],
  "values": 1000000,
  "operator_depths": [1, 8, 32],
  "workloads": ["map", "map-filter", "merge", "bounded-async"],
  "queue_capacity_items": 256,
  "overload": {
    "queue_capacity_items": [4, 64, 1024],
    "pressure_policies": ["block", "reject", "coalesce", "sample", "drop-disposable", "disconnect", "fail"],
    "slow_consumer_yields": 3,
    "branch_count": 1,
    "scope": "finite exact-run single-cord pressure and recovery; persistent ownership is measured separately"
  },
  "fanout": {
    "queue_capacity_items": [4, 64, 1024],
    "branches": [2, 8, 32],
    "modes": ["coupled", "isolated"],
    "slow_branches": ["one", "all"],
    "pressure_policy": "block",
    "slow_consumer_yields": 3,
    "scope": "finite exact-run coupled atomic publication and isolated ordinary-duplicator publication with one profile-accounted retained value"
  },
  "cancellation": {
    "workload": "overload",
    "queue_capacity_items": 4,
    "pressure_policy": "block",
    "stop_policies": ["drain", "abort"],
    "cancel_after_offers": 16,
    "scope": "finite exact-run cancellation requested after block pressure begins"
  },
  "bursty_consumers": {
    "queue_capacity_items": 4,
    "pressure_policies": ["block", "reject", "coalesce", "sample", "drop-disposable"],
    "fanout_branches": [2, 8, 32],
    "fanout_modes": ["coupled", "isolated"],
    "fanout_slow_branches": ["one", "all"],
    "consumer_burst_items": 8,
    "consumer_pause_yields": 8,
    "scope": "finite exact runs alternating explicit cooperative consumer pauses and bounded consume bursts"
  },
  "persistent_sessions": {
    "workload": "overload",
    "queue_capacity_items": 4,
    "pressure_policies": ["block", "reject", "coalesce", "sample", "drop-disposable"],
    "stop_policies": ["drain", "abort"],
    "session_pump_quantum": 8,
    "scope": "production ExactRunSession ownership through repeated bounded host pumps; Block stops with the final offer pending under pressure, while non-blocking policies stop from the standing source's observation boundary"
  },
  "persistent_wake_residency": {
    "workload": "persistent-wake",
    "host_wakes": 10000,
    "residency_plateau_after_wakes": 1000,
    "queue_capacity_items": 4,
    "session_pump_quantum": 8,
    "stop_policy": "drain",
    "scope": "one production ExactRunSession waits for and consumes one exact host-operation wake per value without resetting; queue, payload, ready, and evidence high-water values at the declared checkpoint must equal those after the final wake"
  },
  "persistent_timer_residency": {
    "workload": "persistent-timer",
    "timer_wakes": 10000,
    "residency_plateau_after_wakes": 1000,
    "queue_capacity_items": 4,
    "session_pump_quantum": 8,
    "timer_advance_ticks": 64,
    "stop_policy": "drain",
    "scope": "one production ExactRunSession advances to and consumes one exact retained timer deadline per value without resetting; queue, payload, ready, and evidence high-water values at the declared checkpoint must equal those after the final wake"
  },
  "shared_payload_fanout": {
    "workload": "shared-payload-fanout",
    "payload_bytes": [1024],
    "payload_representation": "hosted-generation-safe-shared-text-handle",
    "branches": [2, 8, 32],
    "termination_requests": ["complete", "abort"],
    "watch_modes": ["none", "one", "every-branch"],
    "watch_preview_bytes": 64,
    "watch_retention": "latest",
    "queue_capacity_items": 1,
    "fanout_mode": "coupled",
    "scope": "production hosted literal value-store handle through an exact coupled fan-out plan assembled by the benchmark harness; zero, one, or every-branch exact-plan-admitted Watch retains a separately accounted 64-byte Latest preview; completion content-verifies every branch, while Abort follows atomic publication and both paths require zero terminal executor value residency"
  },
  "latency_sample_stride": 1024,
  "warmup_trials": 2,
  "measured_trials": 9,
  "runtimes": [
    {
      "id": "conduit-reference-scheduler",
      "version_source": "workspace commit",
      "semantic_boundary": "public exact-plan validator and deterministic executor with handle-backed u64 conformance drivers",
      "demand": "bounded FIFO block",
      "bounded_async": "unavailable in the single-lane reference runner; no hosted streaming result is substituted",
      "streaming_hosted_mode": "unavailable; tracked by #214/#242 and never substituted by the reference result"
    },
    {
      "id": "conduit-hosted-value-arena",
      "version_source": "workspace commit",
      "semantic_boundary": "production hosted std/literal driver, fixed generation-safe value arena, exact coupled fan-out plan, and content-verifying display sinks",
      "demand": "capacity-one lossless cords",
      "payload_boundary": "exactly 1 KiB public text in this slice; larger payloads and other media kinds remain unavailable"
    },
    {
      "id": "rxjs",
      "version": "7.8.2",
      "semantic_boundary": "in-process JavaScript push pipeline",
      "demand": "no Reactive Streams demand",
      "bounded_async": "unavailable; omitted rather than replaced with an uncontrolled observeOn queue"
    },
    {
      "id": "reactor-core",
      "version": "3.8.6",
      "reactive_streams_version": "1.0.4",
      "semantic_boundary": "JVM Reactive Streams pipeline",
      "demand": "subscriber demand; publishOn prefetch is pinned for the bounded asynchronous case"
    }
  ],
  "language_lower_bounds": [
    {
      "id": "rust-identity-loop",
      "for": "conduit-reference-scheduler",
      "claim": "language-cost lower bound only; never a reactive-runtime competitor"
    },
    {
      "id": "javascript-identity-loop",
      "for": "rxjs",
      "claim": "language-cost lower bound only; never a reactive-runtime competitor"
    },
    {
      "id": "java-identity-loop",
      "for": "reactor-core",
      "claim": "language-cost lower bound only; never a reactive-runtime competitor"
    }
  ],
  "strict_invariants": [
    "every runner emits the current raw-sample schema and fixture revision",
    "offered and admitted values equal configured input values for the lossless local-depth fixtures",
    "offered, admitted, completed-useful, rejected, sampled, coalesced, dropped, cancelled, retried, and terminal outcomes are always reported separately",
    "Conduit reports exact scheduler decisions and producer stall duration; Drain and Abort timing stay unavailable until those transitions are explicitly requested",
    "pressured Drain preserves every retained admitted value while pressured Abort may discard retained admitted work and neither coalesced replacement nor aborted work counts as useful",
    "bursty consumers report exact repeated pressure and recovery cycle counts without a hidden timing source or buffer",
    "persistent rows retain one finite session reservation across bounded host pumps, request termination with admitted work still pressured, and release the reservation only after terminal finalization",
    "persistent host-operation and timer residency rows perform the declared deterministic wake count without resetting the session, allocate zero times after Start, reach a strict internal high-water plateau, and reconcile the session reservation after Drain finalization",
    "shared-payload rows retain one generation-safe value handle and exactly one 1 KiB value-arena slot at high water across 2, 8, and 32 branches, deliver and content-verify every branch, allocate zero times after Start, and return value residency to zero after terminal completion",
    "shared-payload Abort rows request cancellation after atomic publication fills every capacity-one branch cord, consume no branch payload, allocate zero times after Start, and return the shared value slot and bytes to zero residency",
    "shared-payload queue charges and display verification buffers scale separately from value-arena residency and are never described as zero-copy",
    "bounded FIFO retries in local-depth fixtures are reported and do not change admitted or completed-useful counts",
    "map, merge, and bounded-async complete one useful output per input",
    "map-filter completes one useful output per input until it contains a filter, then exactly half for the even input cardinality",
    "all sampled values have a positive latency",
    "Conduit does not allocate after Start and never exceeds its exact queue, value, or evidence reservation",
    "overload queue item high water never exceeds the declared capacity",
    "overload accounting conserves offered values according to the exact pressure policy",
    "block, reject, coalesce, sample, and disposable-drop fixtures enter a slow region and then clear into a recovery region",
    "coupled fan-out admits each input atomically to every branch or retries without partial publication",
    "isolated fan-out uses an explicit ordinary duplicator and reports its one retained input through the execution profile",
    "fan-out useful completion equals admitted inputs multiplied by exact branch count",
    "aggregate fan-out queue high water never exceeds capacity multiplied by branch count"
  ],
  "wall_clock_policy": {
    "gate": "strict only for the separately versioned reviewed regression policy when machine class, architecture, input cardinality, and trial counts match exactly; otherwise report-only",
    "summary": "median with deterministic percentile-bootstrap 95% confidence interval over measured trials",
    "minimum_measured_trials": 9,
    "bootstrap_resamples": 10000,
    "seed": 241243,
    "policy": "regression-policy.json",
    "claim_boundary": "a broad alarm requests review and is not a deadline, admission, safety, portability, or performance guarantee"
  }
}
