{
  "schema": "conduit.performance-baseline/v1",
  "owner": "@dancxjo",
  "reviewed_commit": "a45f1d6ba2bfff0e5adfef408e8925c45b7dbf49",
  "rustc": "1.95.0",
  "host_target": "x86_64-unknown-linux-gnu",
  "fixture_revision": 18,
  "threshold_policy": {
    "rule": "fail when growth exceeds the larger of percent and absolute allowances",
    "update": "Run `python3 tools/performance_gate.py --update`, inspect the semantic and artifact-size explanation, then commit the reviewed baseline."
  },
  "artifacts": {
    "conduct-release": {
      "kind": "host-executable",
      "baseline_bytes": 2815656,
      "maximum_growth_percent": 12,
      "maximum_growth_bytes": 262144
    },
    "conduit-core-release": {
      "kind": "host-library-archive",
      "baseline_bytes": 4020696,
      "maximum_growth_percent": 10,
      "maximum_growth_bytes": 131072
    },
    "conduit-core-thumbv6m-release": {
      "kind": "embedded-library-archive-not-flash",
      "baseline_bytes": 3912104,
      "maximum_growth_percent": 10,
      "maximum_growth_bytes": 131072
    }
  },
  "workloads": [
    {
      "id": "canonicalization",
      "command": ["cargo", "test", "--release", "-p", "conduit-core", "--test", "canonical_vectors"],
      "timing": "report-only"
    },
    {
      "id": "compatibility",
      "command": ["cargo", "test", "--release", "-p", "conduit-core", "--test", "compatibility_vectors"],
      "timing": "report-only"
    },
    {
      "id": "panel-parse",
      "command": ["cargo", "test", "--release", "-p", "conduit-panel", "--test", "panel_grammar_vectors"],
      "timing": "report-only"
    },
    {
      "id": "graph-scale-parse",
      "command": ["cargo", "test", "--release", "-p", "conduit-runtime", "--test", "performance_vectors", "small_medium_and_reviewed_maximum_linear_graphs_parse", "--", "--exact"],
      "timing": "report-only",
      "node_counts": [2, 32, 256]
    },
    {
      "id": "source-lowering",
      "command": ["cargo", "test", "--release", "-p", "conduit-runtime", "--test", "source_lowering_vectors"],
      "timing": "report-only"
    },
    {
      "id": "plan-validation",
      "command": ["cargo", "test", "--release", "-p", "conduit-core", "--test", "execution_plan_vectors"],
      "timing": "report-only"
    },
    {
      "id": "scheduler-throughput-latency",
      "command": ["cargo", "test", "--release", "-p", "conduit-runtime", "--test", "scheduler", "long_run_never_exceeds_plan_capacity_and_exposes_metrics", "--", "--exact"],
      "timing": "report-only"
    },
    {
      "id": "fan-out-pressure",
      "command": ["cargo", "test", "--release", "-p", "conduit-runtime", "--test", "scheduler", "coupled_fanout_admits_all_branches_atomically_under_slow_pressure", "--", "--exact"],
      "timing": "report-only"
    },
    {
      "id": "evidence-recording",
      "command": ["cargo", "test", "--release", "-p", "conduit-runtime", "--test", "scheduler", "scheduler_observations_become_bounded_execution_events_on_resonance", "--", "--exact"],
      "timing": "report-only"
    }
  ],
  "deferred_workloads": {
    "tongues": "issue #31",
    "netherwick": "issue #33",
    "rp2040_flash_static_ram_and_stack": "issue #28",
    "plan_transition_overlap": "issue #57"
  }
}
