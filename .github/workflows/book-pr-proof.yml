name: retired-book-pr-proof

# Kept inert for one registry transition: the controller on the current default
# branch still fingerprints this path while admitting the candidate that moves
# browser.tour ownership to executable-book-pages.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Book candidate proof is owned by book-and-creche-products'
