[package]
name = "conduit-bluetooth"
version = "0.1.0"
edition = "2021"

[dependencies]
conduit-core = { path = "../../../architecture/core" }
conduit-wire = { path = "../../../architecture/wire", default-features = false }
serde = { version = "1", default-features = false, features = ["alloc", "derive"] }

[dev-dependencies]
serde_json = "1"
