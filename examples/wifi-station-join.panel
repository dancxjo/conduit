# Wi-Fi station join configuration reference panel.
panel 3

node wifi_sta : net/wifi/join {
    ssid = "ConduitNet"
}

node status_logger : observe/log

cord wifi_sta.state -> status_logger.message {
    capacity = 4
    max_value_bytes = 1024
    max_queued_bytes = 4096
    low_watermark = 1
    high_watermark = 4
    pressure = block
}
