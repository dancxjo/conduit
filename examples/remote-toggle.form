form 0

remote-toggle {
    trigger: interaction/trigger
    toggle: state/toggle
    show: presentation/bool

    trigger.count = 15
    toggle.initial = true

    trigger > toggle
    toggle > show
}
