form 0

remote-toggle {
    trigger: interaction/trigger
    toggle: state/toggle
    show: presentation/show

    trigger.count = 16
    toggle.initial = false

    trigger > toggle
    toggle > show
}
