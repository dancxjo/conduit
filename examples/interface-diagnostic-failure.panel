panel 2

interface speech/recognizer {
  input audio : conduit/text.utf8
  output final : conduit/text.utf8
  output error_channel : conduit/text.utf8
}

# Fails because std/stdout lacks output port `final` and `error_channel`
node invalid_worker : std/stdout implements speech/recognizer
