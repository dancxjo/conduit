[package]
name = "patchbay-browser-runtime"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
conduit-body = { path = "../../../architecture/body" }
conduit-core = { path = "../../../architecture/core" }
patchbay-model = { path = "../model" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
