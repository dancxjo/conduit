{
  "name": "conduit-browser-host-proof",
  "private": true,
  "scripts": {
    "test:browser-host": "playwright test --config hosts/browser/playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
