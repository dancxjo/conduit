[package]
name = "conduit-assigned-plan"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = []
serde = ["dep:serde"]

[dependencies]
serde = { version = "1", default-features = false, features = ["derive"], optional = true }
