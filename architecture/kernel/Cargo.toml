[package]
name = "conduit-kernel"
version = "0.1.0"
edition = "2021"

[features]
default = []
alloc = []
step-test-support = []

[dependencies]
conduit-assigned-plan = { path = "../assigned-plan" }

[dev-dependencies]
conduit-human = { path = "../../semantics/human" }
