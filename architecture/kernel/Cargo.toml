[package]
name = "conduit-kernel"
version = "0.1.0"
edition = "2021"

[features]
default = []
alloc = []

[dependencies]
conduit-core = { path = "../core" }

[dev-dependencies]
conduit-human = { path = "../../semantics/human" }
