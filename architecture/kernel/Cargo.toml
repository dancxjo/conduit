[package]
name = "conduit-kernel"
version = "0.1.0"
edition = "2021"

[features]
default = []
alloc = []

[dev-dependencies]
conduit-human = { path = "../../semantics/human" }
conduit-core = { path = "../core" }
