{
  "schema": "conduit.supervision-fixtures",
  "contract_version": 0,
  "cases": [
    {"id":"domain-outcome-is-value","expected":{"outcome":"domain-value"}},
    {"id":"admission-diagnostic-skips-handler","expected":{"outcome":"admission-diagnostic"}},
    {"id":"terminal-without-handler-propagates","expected":{"outcome":"propagated"}},
    {"id":"bounded-restart-new-attempt","expected":{"outcome":"restart","attempt":2}},
    {"id":"restart-attempts-exhausted","expected":{"code":"CND-SUP-011"}},
    {"id":"retry-non-idempotent-rejected","expected":{"code":"CND-SUP-010"}},
    {"id":"declared-fallback-selected","expected":{"outcome":"fallback"}},
    {"id":"required-degraded-mode-rejected","expected":{"code":"CND-SUP-012"}},
    {"id":"child-race-order-independent","expected":{"primary":"node-failed"}},
    {"id":"cancellation-pending-handler","expected":{"outcome":"cancelled"}},
    {"id":"handler-trap-propagates","expected":{"outcome":"handler-failed"}},
    {"id":"handler-timeout-propagates","expected":{"code":"CND-SUP-008"}},
    {"id":"handler-cancelled-propagates","expected":{"outcome":"cancelled"}},
    {"id":"cleanup-failure-propagates","expected":{"code":"CND-SUP-017"}},
    {"id":"nearest-nested-boundary","expected":{"boundary":"inner"}},
    {"id":"self-supervision-rejected","expected":{"code":"CND-SUP-001"}},
    {"id":"replicated-child-attempt-bounded","expected":{"code":"CND-SUP-011"}},
    {"id":"replacement-is-candidate-epoch","expected":{"code":"CND-SUP-013"}},
    {"id":"authority-expansion-not-an-action","expected":{"code":"CND-SUP-009"}},
    {"id":"hosted-browser-fake-normalized","expected":{"outcome":"propagated"}},
    {"id":"constrained-profile-subset","expected":{"code":"CND-SUP-015"}},
    {"id":"evidence-gap-resync-no-invention","expected":{"cursor":"gap"}},
    {"id":"evidence-exhaustion-before-action","expected":{"code":"CND-SUP-007"}},
    {"id":"observation-limit","expected":{"code":"CND-SUP-004"}},
    {"id":"decision-limit","expected":{"code":"CND-SUP-005"}},
    {"id":"in-flight-limit","expected":{"code":"CND-SUP-006"}},
    {"id":"recovery-deadline-expired","expected":{"code":"CND-SUP-008"}},
    {"id":"panel-supervision-lowers","expected":{"bindings":1}},
    {"id":"exact-plan-resources-required","expected":{"code":"CND-SUP-001"}},
    {"id":"action-changes-plan-identity","expected":{"identity_changed":true}},
    {"id":"unknown-fallback-target-rejected","expected":{"code":"CND-SUP-001"}},
    {"id":"outer-supervision-cycle-rejected","expected":{"code":"CND-SUP-001"}},
    {"id":"domain-negative-can-continue","expected":{"handler_invocations":0}},
    {"id":"handler-cancellation-race","expected":{"primary":"parent-cancelled"}},
    {"id":"old-new-generation-race","expected":{"generation":2}},
    {"id":"operator-wait-is-bounded","expected":{"code":"CND-SUP-008"}},
    {"id":"observation-carries-identities-not-values","expected":{"redacted":true}},
    {"id":"source-self-supervision-rejected","expected":{"code":"CND-SRC-012"}},
    {"id":"compile-roundtrip","expected":{"schema_version":0}},
    {"id":"browser-profile-explicit-action","expected":{"outcome":"restart"}},
    {"id":"allocator-free-profile-equivalent","expected":{"outcome":"propagated"}},
    {"id":"cleanup-policy-is-exact","expected":{"cleanup":"abort"}},
    {"id":"decision-correlation-required","expected":{"code":"CND-SUP-003"}},
    {"id":"hosted-pending-queue-bounded","expected":{"code":"CND-SUP-006"}},
    {"id":"named-group-fail-together","expected":{"affected":"bound-scope","terminal":true}},
    {"id":"named-group-isolated-optional","expected":{"affected":"observed-subject","terminal":false}},
    {"id":"hostile-handler-invents-action-rejected","expected":{"code":"CND-SUP-009"}},
    {"id":"cross-host-decisions-normalized","expected":{"outcome":"restart","attempt":2}}
  ]
}
