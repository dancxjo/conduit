{
  "schema": "conduit.conformance.replicated-pools/v1",
  "cases": [
    {"id":"maximum-one","expected":{"live":1,"second":"capacity"}},
    {"id":"maximum-many","expected":{"live":4,"queued":0}},
    {"id":"admission-reject","expected":{"outcome":"rejected","reason":"capacity"}},
    {"id":"admission-block","expected":{"outcome":"blocked","queued":0}},
    {"id":"admission-queue-bounded","expected":{"outcome":"queued","queued":1}},
    {"id":"admission-fail","expected":{"outcome":"failed","reason":"admission-failed"}},
    {"id":"queue-full","expected":{"outcome":"rejected","reason":"queue-full"}},
    {"id":"queued-cancellation","expected":{"state":"cancelled","cause":true}},
    {"id":"identity-order-independent","expected":{"same":true,"attempt":1}},
    {"id":"cross-pool-identity-distinct","expected":{"distinct":true}},
    {"id":"isolate-failure","expected":{"affected":1,"other":"reserved"}},
    {"id":"fail-together","expected":{"cleanup":2,"disposition":"fail-pool"}},
    {"id":"restart-success","expected":{"attempt":2,"state":"reserved"}},
    {"id":"restart-exhaustion","expected":{"disposition":"restart-exhausted","state":"cleanup"}},
    {"id":"restart-backoff","expected":{"before":"restart-backoff","at":"reserved"}},
    {"id":"explicit-fallback","expected":{"disposition":"fallback","target":"root/fallback"}},
    {"id":"escalation","expected":{"disposition":"escalate","state":"cleanup"}},
    {"id":"idle-timeout","expected":{"reason":"idle-expired","state":"cleanup"}},
    {"id":"deadline-expiry","expected":{"reason":"deadline-expired","state":"cleanup"}},
    {"id":"cleanup-drain","expected":{"terminal":"succeeded","cleanup":"cleanup-drain"}},
    {"id":"cleanup-abort","expected":{"terminal":"failed","cleanup":"cleanup-abort"}},
    {"id":"authority-denial","expected":{"live":0,"reason":"authority-denied"}},
    {"id":"resource-denial","expected":{"live":0,"reason":"reservation-unavailable"}},
    {"id":"implementation-set-denial","expected":{"live":0,"reason":"implementation-mismatch"}},
    {"id":"pressure-isolation","expected":{"failed":"cleanup","pressure":"loss","cause":true,"other":"running"}},
    {"id":"checkpoint-compatible","expected":{"accepted":true,"state":"checkpointing"}},
    {"id":"checkpoint-incompatible","expected":{"accepted":false,"state":"running"}},
    {"id":"pool-cancellation-causality","expected":{"caused":2,"cause_exact":true}},
    {"id":"generation-overlap","expected":{"valid":true,"reserved_slots":4}},
    {"id":"generation-overlap-denied","expected":{"code":"CND-POL-005"}},
    {"id":"generation-drain","expected":{"retiring":2,"queued":0}},
    {"id":"generation-rollback","expected":{"cleanup":2,"cancelled":1}},
    {"id":"malicious-profile","expected":{"accepted":false,"reason":"foreign-profile-exceeded"}},
    {"id":"long-run-population","expected":{"ticks":10000,"maximum_live":2,"maximum_queued":2}},
    {"id":"fair-pool-selection","expected":{"order":[1,2,1]}},
    {"id":"evidence-exhaustion-before-mutation","expected":{"code":"CND-POL-006","unchanged":true}},
    {"id":"legacy-plan-not-executable","expected":{"code":"CND-POOL-HOST-002"}},
    {"id":"schema-16-roundtrip","expected":{"schema_version":16,"runtime":true}}
  ]
}
