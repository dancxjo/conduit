[package]
name = "conduit-ai"
description = "Bounded domain AI/chat contracts and local provider adapters for Conduit"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
conduit-core = { path = "../conduit-core" }
conduit-panel = { path = "../conduit-panel" }
conduit-runtime = { path = "../conduit-runtime" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
sha2 = "0.10.9"

[dev-dependencies]
bumpalo = "3.19.0"
conduit-compile = { path = "../conduit-compile" }
tempfile = "3.27.0"

[lints]
workspace = true
