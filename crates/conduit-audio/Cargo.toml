[package]
name = "conduit-audio"
version = "0.1.0"
edition = "2021"

[dependencies]
conduit-core = { path = "../conduit-core" }
libm = "0.2"
serde = { version = "1", default-features = false, features = ["alloc", "derive"] }
