[package]
name = "conduit-runtime"
description = "Hosted Conduit registry, resolver, explainer, and executor"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
conduit-core = { path = "../conduit-core" }
conduit-panel = { path = "../conduit-panel" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
sha2 = "0.10.9"

[dev-dependencies]
conduit-embedded = { path = "../conduit-embedded" }
conduit-package = { path = "../conduit-package" }
conduit-rp2040-hil = { path = "../../firmware/conduit-rp2040-hil" }

[lints]
workspace = true
