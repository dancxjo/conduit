[package]
name = "conduit-process"
description = "Bounded opt-in hosted process exec provider for Conduit"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
conduit-core = { path = "../conduit-core" }
conduit-panel = { path = "../conduit-panel" }
conduit-runtime = { path = "../conduit-runtime" }
conduit-std = { path = "../conduit-std" }

[target.'cfg(unix)'.dependencies]
nix = { version = "0.31.2", features = ["process", "signal"] }

[lints]
workspace = true
