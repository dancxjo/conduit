[package]
name = "conduit-alife"
version = "0.1.0"
edition = "2021"

[dependencies]
conduit-core = { path = "../conduit-core" }
conduit-form = { path = "../conduit-form" }
conduit-presentation = { path = "../conduit-presentation", default-features = false, features = ["form-catalog"] }
conduit-time = { path = "../conduit-time" }
sha2 = { version = "0.10", default-features = false }

[dev-dependencies]
conduit-planner = { path = "../conduit-planner" }
conduit-wire = { path = "../conduit-wire" }
